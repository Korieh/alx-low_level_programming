#include "main.h"

/**
 * _srtcmp - function to compare two strings
 * @s1: first string
 * @s2: second string
 * Return: outputs the difference of s1 and s2
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
